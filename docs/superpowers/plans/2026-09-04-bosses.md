# Chefes (Plano 5a de 6) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar cada memória num confronto: um chefe surge após um tempo, com máquina de estados (intro → fase 1 → fase 2 → enfurecido → morto), padrões de ataque (anel de projéteis, investida, invocação, nova), e **matar o chefe é a condição de vitória** (o timer vira só uma rede de segurança). Cinco chefes definidos como dados — Cavaleiro Profanador, Inquisidor-Mor, Comandante Janízaro, O Primeiro Traído e Satã (3 fases) — com o de M1 usando a arte real e os demais um placeholder.

**Architecture:** Segue a fundação — lógica pura testada no Vitest, Phaser só nas cenas. O chefe é uma entidade única em `world.boss` (não pooled). `BossSystem` faz spawn no tempo certo, roda a máquina de estados, move o chefe e executa os ataques (que reusam o pool de `Attack` com `ownerPowerId: 'boss'` e o pool de `Enemy` para invocações). `AttackCollisionSystem`/`PlayerAttackSystem`/`ContactDamageSystem` ganham o lado do chefe: ataques do jogador ferem o chefe, ataques do chefe (`ownerPowerId: 'boss'`) ferem o jogador. `runOutcome` passa a devolver `'victory'` quando `world.bossDefeated`.

**Tech Stack:** Vite 5, Phaser 3.80, TypeScript 5.4 (strict), Vitest 1.6.

## Global Constraints

Herdadas de [`docs/DESIGN.md`](../../DESIGN.md). Todo task herda esta seção.

- Stack: **Phaser 3 + TypeScript**; build web estática. **Sem dependências de runtime além do `phaser`**.
- Alvo **60 FPS**. Teto rígido: `MAX_ENEMIES 350`, `MAX_PROJECTILES 1000` (pool de `Attack`), `MAX_PICKUPS 800`. `acquire()` → `null` quando cheio, tratado sem estourar.
- **RNG com seed por run** (`world.rng`) para ângulos de spawn/ataque; nada de `Math.random()` na lógica.
- Ataques do chefe reusam o pool de `Attack` com `ownerPowerId: 'boss'`; **o `PowerSystem` nunca libera um `Attack` que não seja de um poder conhecido** (senão uma troca de poder apagaria os projéteis do chefe).
- Chefe é **invulnerável durante a `intro`**.
- Vitória da memória = **chefe derrotado** (`world.bossDefeated`); o `durationSec` da memória continua como teto de segurança (também conta como vitória).
- Textos em pt-BR próximos do código. HUD real (barra de vida do chefe) é do Plano 5b — aqui a `RunScene` mostra o estado do chefe no texto de depuração.
- Arte real só para o chefe de M1 (`sprites/boss_m1_profaner_knight.jpg` → `boss-m1`); os outros usam a textura placeholder `dev-boss`.
- `tsc --noEmit`, `vitest run` e `vite build` verdes ao fim de cada task.

## Estado herdado (não reimplementar)

- Planos 1–4 completos. Pontos relevantes:
  - `src/entities/Player.ts` — `interface Vec2`, `class Player { pos; vel; intent; stats: Stats; hp; radius; invulnUntilMs }`.
  - `src/entities/Enemy.ts` — `class Enemy implements Poolable`, `spawn(def,x,y)`; `src/data/enemies.ts` — `ENEMY_DEFS`, `type EnemyArchetype`.
  - `src/entities/Attack.ts` — `class Attack implements Poolable` com `pos,vel,radius,damage,pierceLeft,hitCooldownMs,lifespanMs,ageMs,motion,orbitAngle,orbitRadius,orbitSpeed,ownerPowerId,spriteKey,hits: Map<Enemy,number>`. **Ganha `bossHitAtMs` na Task 3.**
  - `src/core/EventBus.ts` — `interface EventMap` com `enemy:died`, `player:levelup`, `player:damaged`, `player:died`, `stats:dirty`. **Ganha eventos de chefe na Task 2.**
  - `src/world/World.ts` — `interface World { rng; events; time; camera; player; enemies: Pool<Enemy>; pickups: Pool<Pickup>; attacks: Pool<Attack>; powers: PowerRoster; progression; draftPool: string[] }`; `createWorld(seed)`. **Ganha `boss` e `bossDefeated` na Task 2.**
  - `src/systems/*` — `System` (`update(world, deltaMs)`), `SpawnDirector` (re-exporta `MEMORY_PLACEHOLDER`), `PowerSystem`, `AttackMotionSystem`, `AttackCollisionSystem`, `PlayerAttackSystem`, `ContactDamageSystem`, `EnemyMovementSystem`, `PickupSystem`, `RegenSystem`, `MovementSystem`, `DashSystem`, `InputSystem`, `CameraSystem`.
  - `src/combat/kill.ts` — `killEnemy(world, enemy)`.
  - `src/data/powers.ts` — `POWER_DEFS`, `powerLevel`, `BASE_DRAFT_POOL`.
  - `src/data/memories.ts` — `interface MemoryDef { id; name; durationSec; rewardPowerId?; timeline }`, `MEMORIES` (3), `MEMORY_PLACEHOLDER = MEMORIES[0]`, `memoryUnlocked`. **`MemoryDef` ganha `bossId`/`bossTimeSec` na Task 7.**
  - `src/run/runEnd.ts` — `type RunOutcome`, `runOutcome(world, memoryDurationSec)`. **Atualizado na Task 7.**
  - `src/config/gameConfig.ts` — `SPAWN_RING_RADIUS`, `IFRAME_MS`, `CLAW_RANGE`, `CLAW_DAMAGE`, `AURA_TEX_SIZE`.
  - `src/scenes/{Boot,Preload,Hub,Run,Upgrade,RunEnd}Scene.ts`; `tools/process-sprites.mjs` (JOBS + `manifest.json`), `public/sprites/*`.
  - `tests/helpers/headlessWorld.ts` — `makeWorld(seed?)`, `tick(world, systems, frames, stepMs?)`.

**Verificação no navegador:** a Browser pane não roda `requestAnimationFrame`. Bombeie o frame com `window.__GAME__.step(t, dt)` num laço (roda update + render + loader). Fronte a aba; sem `await` dentro do laço.

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `src/entities/Boss.ts` | `type BossPhase`, `class Boss` (dados puros; `spawn(def,x,y)`, `reset()`). |
| `src/data/bosses.ts` | `type BossAttackKind`, `interface BossAttack`, `interface BossPhaseDef`, `interface BossDef`, `const BOSS_DEFS` (5). |
| `src/combat/boss.ts` | `damageBoss(world, amount)` — aplica dano, emite `boss:died` e seta `world.bossDefeated`. |
| `src/systems/BossSystem.ts` | Spawn no tempo, máquina de estados, movimento, execução de ataques. |
| `src/core/EventBus.ts` | *(editar)* `EventMap` ganha `boss:spawned`, `boss:phase`, `boss:died`. |
| `src/entities/Attack.ts` | *(editar)* `bossHitAtMs` (último acerto no chefe, para hitbox persistente). |
| `src/world/World.ts` | *(editar)* `World` ganha `boss: Boss` e `bossDefeated: boolean`. |
| `src/systems/PowerSystem.ts` | *(editar)* `resync` só libera `Attack`s cujo `ownerPowerId` é um poder conhecido. |
| `src/systems/AttackCollisionSystem.ts` | *(editar)* ataques `boss` ferem o jogador; ataques do jogador ferem também o chefe. |
| `src/systems/PlayerAttackSystem.ts` | *(editar)* a garra também atinge o chefe. |
| `src/systems/ContactDamageSystem.ts` | *(editar)* encostar no chefe ativo causa dano de contato. |
| `src/systems/SpawnDirector.ts` | *(editar)* não surge inimigos comuns enquanto `world.boss.active`. |
| `src/data/memories.ts` | *(editar)* `MemoryDef` ganha `bossId`/`bossTimeSec`; as 3 memórias recebem chefe. |
| `src/run/runEnd.ts` | *(editar)* `runOutcome` devolve `'victory'` quando `world.bossDefeated`. |
| `src/scenes/PreloadScene.ts` | *(editar)* textura placeholder `dev-boss`. |
| `src/scenes/RunScene.ts` | *(editar)* `BossSystem` na lista; sprite do chefe; texto de depuração com o estado do chefe. |
| `tools/process-sprites.mjs` | *(editar)* job `boss_m1_profaner_knight.jpg` → `boss-m1`. |
| `tests/**/*.test.ts` | Testes por módulo + integração do confronto. |

---

### Task 1: Entidade Boss + BOSS_DEFS

**Files:**
- Create: `src/entities/Boss.ts`
- Create: `src/data/bosses.ts`
- Test: `tests/entities/boss.test.ts`
- Test: `tests/data/bosses.test.ts`

**Interfaces:**
- Consumes: `Vec2` (`entities/Player`), `EnemyArchetype` (`data/enemies`).
- Produces:
  - `type BossPhase = 'intro' | 'p1' | 'p2' | 'enraged' | 'dead'`
  - `type BossAttackKind = 'ring' | 'charge' | 'summon' | 'nova'`
  - `interface BossAttack { kind: BossAttackKind; cooldownMs: number; damage: number; count?: number; speed?: number; radius?: number; archetype?: EnemyArchetype }`
  - `interface BossPhaseDef { moveSpeed: number; attacks: BossAttack[] }`
  - `interface BossDef { id: string; name: string; hp: number; radius: number; contactDamage: number; spriteKey: string; introMs: number; p2At: number; enrageAt: number; phases: [BossPhaseDef, BossPhaseDef, BossPhaseDef] }` — `p2At`/`enrageAt` são frações de `hp/maxHp` (ex.: 0.66 / 0.33).
  - `const BOSS_DEFS: Record<string, BossDef>` — chaves `profaner-knight` (spriteKey `'boss-m1'`), `grand-inquisitor`, `janissary-commander`, `the-first-betrayed`, `satan` (todos os demais spriteKey `'dev-boss'`). Valores dos steps.
  - `class Boss { active: boolean; readonly pos: Vec2; defId: string; hp: number; maxHp: number; radius: number; contactDamage: number; moveSpeed: number; phase: BossPhase; phaseTimeMs: number; attackCdMs: number; attackIndex: number; spawn(def: BossDef, x: number, y: number): void; reset(): void }` — `spawn` seta tudo, `phase = 'intro'`, `active = true`. `reset` zera e `active = false`.

- [ ] **Step 1: Escrever os testes que falham**

```ts
// tests/entities/boss.test.ts
import { describe, it, expect } from 'vitest';
import { Boss } from '../../src/entities/Boss';
import { BOSS_DEFS } from '../../src/data/bosses';

describe('Boss', () => {
  it('nasce inativo e zerado', () => {
    const b = new Boss();
    expect(b.active).toBe(false);
    expect(b.hp).toBe(0);
    expect(b.phase).toBe('intro');
  });

  it('spawn() configura a partir do def e ativa na intro', () => {
    const b = new Boss();
    const def = BOSS_DEFS['profaner-knight'];
    b.spawn(def, 100, -40);
    expect(b.active).toBe(true);
    expect(b.defId).toBe('profaner-knight');
    expect(b.pos).toEqual({ x: 100, y: -40 });
    expect(b.hp).toBe(def.hp);
    expect(b.maxHp).toBe(def.hp);
    expect(b.radius).toBe(def.radius);
    expect(b.phase).toBe('intro');
  });

  it('reset() volta ao estado inativo sem trocar a ref de pos', () => {
    const b = new Boss();
    const posRef = b.pos;
    b.spawn(BOSS_DEFS['satan'], 5, 5);
    b.reset();
    expect(b.pos).toBe(posRef);
    expect(b.active).toBe(false);
    expect(b.hp).toBe(0);
  });
});
```

```ts
// tests/data/bosses.test.ts
import { describe, it, expect } from 'vitest';
import { BOSS_DEFS } from '../../src/data/bosses';

describe('BOSS_DEFS', () => {
  it('tem os 5 chefes, cada um com 3 fases e limiares decrescentes', () => {
    for (const id of ['profaner-knight', 'grand-inquisitor', 'janissary-commander', 'the-first-betrayed', 'satan']) {
      const def = BOSS_DEFS[id];
      expect(def.id).toBe(id);
      expect(def.hp).toBeGreaterThan(0);
      expect(def.phases).toHaveLength(3);
      for (const ph of def.phases) expect(ph.attacks.length).toBeGreaterThan(0);
      expect(def.p2At).toBeGreaterThan(def.enrageAt);
      expect(def.enrageAt).toBeGreaterThan(0);
    }
  });

  it('só o chefe de M1 usa arte real', () => {
    expect(BOSS_DEFS['profaner-knight'].spriteKey).toBe('boss-m1');
    expect(BOSS_DEFS['satan'].spriteKey).toBe('dev-boss');
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `npx vitest run tests/entities/boss.test.ts tests/data/bosses.test.ts`
Expected: FAIL — módulos não encontrados.

- [ ] **Step 3: Implementar `src/data/bosses.ts`**

```ts
import type { EnemyArchetype } from './enemies';

export type BossPhase = 'intro' | 'p1' | 'p2' | 'enraged' | 'dead';
export type BossAttackKind = 'ring' | 'charge' | 'summon' | 'nova';

export interface BossAttack {
  kind: BossAttackKind;
  cooldownMs: number;
  damage: number;
  /** ring: nº de projéteis; summon: nº de inimigos. */
  count?: number;
  /** ring: velocidade do projétil; charge: velocidade da investida (px/s). */
  speed?: number;
  /** ring: raio do projétil; nova: raio da explosão. */
  radius?: number;
  /** summon: arquétipo invocado. */
  archetype?: EnemyArchetype;
}

export interface BossPhaseDef {
  moveSpeed: number;
  attacks: BossAttack[];
}

export interface BossDef {
  id: string;
  name: string;
  hp: number;
  radius: number;
  contactDamage: number;
  spriteKey: string;
  introMs: number;
  /** entra na fase 2 quando hp/maxHp <= p2At. */
  p2At: number;
  /** entra em 'enraged' quando hp/maxHp <= enrageAt. */
  enrageAt: number;
  phases: [BossPhaseDef, BossPhaseDef, BossPhaseDef];
}

const ring = (o: Partial<BossAttack> = {}): BossAttack => ({
  kind: 'ring',
  cooldownMs: 2600,
  damage: 12,
  count: 12,
  speed: 90,
  radius: 5,
  ...o,
});
const charge = (o: Partial<BossAttack> = {}): BossAttack => ({
  kind: 'charge',
  cooldownMs: 3200,
  damage: 20,
  speed: 260,
  radius: 8,
  ...o,
});
const summon = (o: Partial<BossAttack> = {}): BossAttack => ({
  kind: 'summon',
  cooldownMs: 4000,
  damage: 0,
  count: 4,
  archetype: 'runner',
  ...o,
});
const nova = (o: Partial<BossAttack> = {}): BossAttack => ({
  kind: 'nova',
  cooldownMs: 3400,
  damage: 18,
  radius: 46,
  ...o,
});

export const BOSS_DEFS: Record<string, BossDef> = {
  'profaner-knight': {
    id: 'profaner-knight',
    name: 'Cavaleiro Profanador',
    hp: 600,
    radius: 12,
    contactDamage: 14,
    spriteKey: 'boss-m1',
    introMs: 1400,
    p2At: 0.66,
    enrageAt: 0.33,
    phases: [
      { moveSpeed: 34, attacks: [charge()] },
      { moveSpeed: 40, attacks: [charge({ cooldownMs: 2800 }), ring({ count: 10 })] },
      { moveSpeed: 48, attacks: [charge({ cooldownMs: 2200 }), ring({ count: 14, cooldownMs: 2200 })] },
    ],
  },
  'grand-inquisitor': {
    id: 'grand-inquisitor',
    name: 'Inquisidor-Mor',
    hp: 800,
    radius: 13,
    contactDamage: 12,
    spriteKey: 'dev-boss',
    introMs: 1400,
    p2At: 0.66,
    enrageAt: 0.33,
    phases: [
      { moveSpeed: 22, attacks: [nova()] },
      { moveSpeed: 24, attacks: [nova({ cooldownMs: 3000 }), summon({ archetype: 'crawler' })] },
      { moveSpeed: 26, attacks: [nova({ cooldownMs: 2400, radius: 58 }), summon({ count: 6 })] },
    ],
  },
  'janissary-commander': {
    id: 'janissary-commander',
    name: 'Comandante Janízaro',
    hp: 1000,
    radius: 14,
    contactDamage: 16,
    spriteKey: 'dev-boss',
    introMs: 1600,
    p2At: 0.6,
    enrageAt: 0.3,
    phases: [
      { moveSpeed: 30, attacks: [charge({ speed: 300 })] },
      { moveSpeed: 34, attacks: [charge({ speed: 320, cooldownMs: 2800 }), summon()] },
      { moveSpeed: 40, attacks: [charge({ speed: 360, cooldownMs: 2200 }), summon({ count: 6 }), ring({ count: 12 })] },
    ],
  },
  'the-first-betrayed': {
    id: 'the-first-betrayed',
    name: 'O Primeiro Traído',
    hp: 900,
    radius: 12,
    contactDamage: 14,
    spriteKey: 'dev-boss',
    introMs: 1400,
    p2At: 0.66,
    enrageAt: 0.33,
    phases: [
      { moveSpeed: 40, attacks: [ring()] },
      { moveSpeed: 46, attacks: [ring({ count: 16 }), nova()] },
      { moveSpeed: 54, attacks: [ring({ count: 20, cooldownMs: 2000 }), nova({ cooldownMs: 2400 })] },
    ],
  },
  satan: {
    id: 'satan',
    name: 'Satã',
    hp: 3000,
    radius: 18,
    contactDamage: 22,
    spriteKey: 'dev-boss',
    introMs: 2000,
    p2At: 0.66,
    enrageAt: 0.33,
    phases: [
      { moveSpeed: 20, attacks: [ring({ count: 16, damage: 16 }), nova({ damage: 24 })] },
      { moveSpeed: 24, attacks: [ring({ count: 20, damage: 18 }), charge({ damage: 28 }), summon({ count: 5 })] },
      { moveSpeed: 30, attacks: [ring({ count: 26, cooldownMs: 1800, damage: 22 }), charge({ cooldownMs: 2200, damage: 34 }), nova({ radius: 70, cooldownMs: 2600, damage: 34 }), summon({ count: 8 })] },
    ],
  },
};
```

- [ ] **Step 4: Implementar `src/entities/Boss.ts`**

```ts
import type { Vec2 } from './Player';
import type { BossDef, BossPhase } from '../data/bosses';

export type { BossPhase } from '../data/bosses';

/** Chefe da memória. Instância única em `world.boss` — não é pooled. */
export class Boss {
  active = false;
  readonly pos: Vec2 = { x: 0, y: 0 };
  defId = '';
  hp = 0;
  maxHp = 0;
  radius = 0;
  contactDamage = 0;
  moveSpeed = 0;
  phase: BossPhase = 'intro';
  phaseTimeMs = 0;
  attackCdMs = 0;
  attackIndex = 0;

  spawn(def: BossDef, x: number, y: number): void {
    this.defId = def.id;
    this.pos.x = x;
    this.pos.y = y;
    this.hp = def.hp;
    this.maxHp = def.hp;
    this.radius = def.radius;
    this.contactDamage = def.contactDamage;
    this.moveSpeed = 0;
    this.phase = 'intro';
    this.phaseTimeMs = 0;
    this.attackCdMs = 0;
    this.attackIndex = 0;
    this.active = true;
  }

  reset(): void {
    this.defId = '';
    this.pos.x = 0;
    this.pos.y = 0;
    this.hp = 0;
    this.maxHp = 0;
    this.radius = 0;
    this.contactDamage = 0;
    this.moveSpeed = 0;
    this.phase = 'intro';
    this.phaseTimeMs = 0;
    this.attackCdMs = 0;
    this.attackIndex = 0;
    this.active = false;
  }
}
```

- [ ] **Step 5: Rodar e confirmar que passam**

Run: `npx vitest run tests/entities/boss.test.ts tests/data/bosses.test.ts`
Expected: PASS — 3 + 2 testes verdes.

- [ ] **Step 6: Commit**

```bash
git add src/entities/Boss.ts src/data/bosses.ts tests/entities/boss.test.ts tests/data/bosses.test.ts
git commit -m "feat(boss): Boss entity + 5 boss definitions (incl. Satan 3 phases)"
```

---

### Task 2: World.boss + eventos de chefe

**Files:**
- Modify: `src/core/EventBus.ts`
- Modify: `src/world/World.ts`
- Test: `tests/world/world-boss.test.ts`

**Interfaces:**
- Consumes: `Boss` (Task 1).
- Produces:
  - `EventMap` ganha: `'boss:spawned': { defId: string }`, `'boss:phase': { phase: string }`, `'boss:died': Record<string, never>`.
  - `World` ganha `boss: Boss` (nova instância) e `bossDefeated: boolean` (`false`).
  - `createWorld` inicia `boss: new Boss()`, `bossDefeated: false`.

- [ ] **Step 1: Escrever `tests/world/world-boss.test.ts` (falha)**

```ts
import { describe, it, expect, vi } from 'vitest';
import { createWorld } from '../../src/world/World';

describe('World.boss', () => {
  it('começa com um chefe inativo e bossDefeated false', () => {
    const world = createWorld(1);
    expect(world.boss.active).toBe(false);
    expect(world.bossDefeated).toBe(false);
  });

  it('o EventBus aceita os eventos de chefe', () => {
    const world = createWorld(1);
    const spawned = vi.fn();
    const died = vi.fn();
    world.events.on('boss:spawned', spawned);
    world.events.on('boss:died', died);
    world.events.emit('boss:spawned', { defId: 'satan' });
    world.events.emit('boss:died', {});
    expect(spawned).toHaveBeenCalledWith({ defId: 'satan' });
    expect(died).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/world/world-boss.test.ts`
Expected: FAIL — `world.boss` indefinido / evento não tipado.

- [ ] **Step 3: Editar `src/core/EventBus.ts`**

Na `interface EventMap`, acrescentar:

```ts
  'boss:spawned': { defId: string };
  'boss:phase': { phase: string };
  'boss:died': Record<string, never>;
```

- [ ] **Step 4: Editar `src/world/World.ts`**

Import:
```ts
import { Boss } from '../entities/Boss';
```
No `interface World`, após `draftPool: string[];`:
```ts
  boss: Boss;
  bossDefeated: boolean;
```
Em `createWorld`, após `draftPool: [...BASE_DRAFT_POOL],`:
```ts
    boss: new Boss(),
    bossDefeated: false,
```

- [ ] **Step 5: Rodar e confirmar que passa + suíte + typecheck**

Run: `npx vitest run tests/world/world-boss.test.ts`
Expected: PASS.

Run: `npm test && npm run typecheck`
Expected: tudo verde.

- [ ] **Step 6: Commit**

```bash
git add src/core/EventBus.ts src/world/World.ts tests/world/world-boss.test.ts
git commit -m "feat(boss): world.boss + bossDefeated + boss events"
```

---

### Task 3: damageBoss + guarda do PowerSystem

**Files:**
- Modify: `src/entities/Attack.ts` (`bossHitAtMs`)
- Create: `src/combat/boss.ts`
- Modify: `src/systems/PowerSystem.ts` (guarda no `resync`)
- Test: `tests/combat/boss.test.ts`
- Test: `tests/systems/powerSystem-bossguard.test.ts`

**Interfaces:**
- Consumes: `World`, `Boss`, `POWER_DEFS` (`data/powers`).
- Produces:
  - `Attack` ganha `bossHitAtMs: number` (default `-Infinity`; `reset()` volta a `-Infinity`).
  - `function damageBoss(world: World, amount: number): void` — se `!world.boss.active` ou `phase` é `'intro'`/`'dead'`, não faz nada. Senão `boss.hp -= amount`; se `hp <= 0`: `hp = 0`, `phase = 'dead'`, `active = false`, `world.bossDefeated = true`, `emit('boss:died', {})`.
  - `PowerSystem.resync` passa a liberar um `Attack` ativo só quando `a.ownerPowerId !== '' && POWER_DEFS[a.ownerPowerId] !== undefined` (ataques de chefe, `ownerPowerId: 'boss'`, sobrevivem).

- [ ] **Step 1: Escrever os testes que falham**

```ts
// tests/combat/boss.test.ts
import { describe, it, expect, vi } from 'vitest';
import { createWorld } from '../../src/world/World';
import { damageBoss } from '../../src/combat/boss';
import { BOSS_DEFS } from '../../src/data/bosses';

describe('damageBoss', () => {
  it('não fere o chefe na intro', () => {
    const world = createWorld(1);
    world.boss.spawn(BOSS_DEFS['profaner-knight'], 0, 0); // phase 'intro'
    damageBoss(world, 100);
    expect(world.boss.hp).toBe(BOSS_DEFS['profaner-knight'].hp);
  });

  it('fere o chefe fora da intro', () => {
    const world = createWorld(1);
    world.boss.spawn(BOSS_DEFS['profaner-knight'], 0, 0);
    world.boss.phase = 'p1';
    damageBoss(world, 100);
    expect(world.boss.hp).toBe(BOSS_DEFS['profaner-knight'].hp - 100);
  });

  it('hp <= 0 => morto, inativo, bossDefeated, evento', () => {
    const world = createWorld(1);
    const died = vi.fn();
    world.events.on('boss:died', died);
    world.boss.spawn(BOSS_DEFS['profaner-knight'], 0, 0);
    world.boss.phase = 'p1';
    damageBoss(world, 99999);
    expect(world.boss.hp).toBe(0);
    expect(world.boss.active).toBe(false);
    expect(world.boss.phase).toBe('dead');
    expect(world.bossDefeated).toBe(true);
    expect(died).toHaveBeenCalledTimes(1);
  });
});
```

```ts
// tests/systems/powerSystem-bossguard.test.ts
import { describe, it, expect } from 'vitest';
import { createWorld } from '../../src/world/World';
import { PowerSystem } from '../../src/systems/PowerSystem';

describe('PowerSystem resync não apaga ataques de chefe', () => {
  it('mantém Attacks com ownerPowerId "boss" ao trocar de poder', () => {
    const world = createWorld(1);
    world.powers.equip('bat-swarm');
    const sys = new PowerSystem();
    sys.update(world, 16); // materializa orbes do bat-swarm

    const bossBolt = world.attacks.acquire()!;
    bossBolt.ownerPowerId = 'boss';
    bossBolt.motion = 'linear';

    world.powers.levelUp('bat-swarm'); // muda a revision -> resync
    sys.update(world, 16);

    let bossAttacks = 0;
    world.attacks.forEachActive((a) => {
      if (a.ownerPowerId === 'boss') bossAttacks++;
    });
    expect(bossAttacks).toBe(1);
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `npx vitest run tests/combat/boss.test.ts tests/systems/powerSystem-bossguard.test.ts`
Expected: FAIL — módulo ausente / o resync ainda apaga o ataque do chefe.

- [ ] **Step 3: Editar `src/entities/Attack.ts`**

Adicionar o campo (junto de `hits`):

```ts
  /** ms do último acerto no chefe (hitbox persistente). */
  bossHitAtMs = -Infinity;
```

E em `reset()`, junto de `this.hits.clear();`:

```ts
    this.bossHitAtMs = -Infinity;
```

- [ ] **Step 4: Implementar `src/combat/boss.ts`**

```ts
import type { World } from '../world/World';

/** Aplica dano ao chefe. Ignora enquanto ele está na intro ou já morto. */
export function damageBoss(world: World, amount: number): void {
  const b = world.boss;
  if (!b.active || b.phase === 'intro' || b.phase === 'dead') return;
  b.hp -= amount;
  if (b.hp <= 0) {
    b.hp = 0;
    b.phase = 'dead';
    b.active = false;
    world.bossDefeated = true;
    world.events.emit('boss:died', {});
  }
}
```

- [ ] **Step 5: Editar `src/systems/PowerSystem.ts`**

No método `resync`, trocar:

```ts
    world.attacks.forEachActive((a) => {
      if (a.ownerPowerId !== '') world.attacks.release(a);
    });
```

por:

```ts
    world.attacks.forEachActive((a) => {
      if (a.ownerPowerId !== '' && POWER_DEFS[a.ownerPowerId] !== undefined) {
        world.attacks.release(a);
      }
    });
```

(`POWER_DEFS` já está importado no arquivo.)

- [ ] **Step 6: Rodar e confirmar que passam + suíte + typecheck**

Run: `npx vitest run tests/combat/boss.test.ts tests/systems/powerSystem-bossguard.test.ts`
Expected: PASS.

Run: `npm test && npm run typecheck`
Expected: tudo verde.

- [ ] **Step 7: Commit**

```bash
git add src/entities/Attack.ts src/combat/boss.ts src/systems/PowerSystem.ts tests/combat/boss.test.ts tests/systems/powerSystem-bossguard.test.ts
git commit -m "feat(boss): damageBoss helper + guard PowerSystem resync from boss attacks"
```

---

### Task 4: BossSystem — spawn, máquina de estados, movimento

**Files:**
- Create: `src/systems/BossSystem.ts`
- Test: `tests/systems/bossSystem-state.test.ts`

**Interfaces:**
- Consumes: `System`, `World`, `Boss`/`BossPhase` (Task 1), `BOSS_DEFS` (Task 1), `SPAWN_RING_RADIUS` (`gameConfig`).
- Produces:
  - `class BossSystem implements System { constructor(bossId: string, bossTimeSec: number); update(world: World, deltaMs: number): void }`:
    - `spawned` (privado, `false`). Quando `!spawned && world.time.elapsedMs >= bossTimeSec * 1000`: `spawned = true`; sorteia um ângulo com `world.rng.next()`; `world.boss.spawn(BOSS_DEFS[bossId], player.x + cos*SPAWN_RING_RADIUS, player.y + sin*SPAWN_RING_RADIUS)`; `emit('boss:spawned', { defId: bossId })`.
    - Se `!world.boss.active`, retorna.
    - `boss.phaseTimeMs += deltaMs`.
    - `phase === 'intro'`: quando `phaseTimeMs >= def.introMs` → `enterPhase(boss, 'p1', def.phases[0])` (que zera `phaseTimeMs`/`attackIndex`, seta `moveSpeed`, `attackCdMs = attacks[0].cooldownMs`, emite `boss:phase`). Não move nem ataca na intro.
    - `phase === 'dead'`: retorna.
    - Transição por hp: `frac = boss.hp / boss.maxHp`; se `phase === 'p1' && frac <= def.p2At` → `enterPhase('p2', phases[1])`; senão se `(phase === 'p1' || phase === 'p2') && frac <= def.enrageAt` → `enterPhase('enraged', phases[2])`.
    - Movimento: anda `moveSpeed * dt` em direção ao jogador, parando a `boss.radius + player.radius + 4`.
    - Ataques entram na Task 5 (aqui `attackCdMs` só decrementa; nenhum efeito ainda).

- [ ] **Step 1: Escrever `tests/systems/bossSystem-state.test.ts` (falha)**

```ts
import { describe, it, expect, vi } from 'vitest';
import { createWorld, advanceTime } from '../../src/world/World';
import { BossSystem } from '../../src/systems/BossSystem';
import { BOSS_DEFS } from '../../src/data/bosses';

function run(world: ReturnType<typeof createWorld>, sys: BossSystem, ms: number, step = 16): void {
  for (let t = 0; t < ms; t += step) {
    advanceTime(world, step);
    sys.update(world, step);
  }
}

describe('BossSystem — estados', () => {
  it('surge o chefe no bossTimeSec e emite boss:spawned', () => {
    const world = createWorld(1);
    const spawned = vi.fn();
    world.events.on('boss:spawned', spawned);
    const sys = new BossSystem('profaner-knight', 2);
    run(world, sys, 1900);
    expect(world.boss.active).toBe(false);
    run(world, sys, 300);
    expect(world.boss.active).toBe(true);
    expect(world.boss.defId).toBe('profaner-knight');
    expect(spawned).toHaveBeenCalledWith({ defId: 'profaner-knight' });
  });

  it('sai da intro para p1 após introMs', () => {
    const world = createWorld(1);
    const sys = new BossSystem('profaner-knight', 0);
    run(world, sys, 16); // spawn
    expect(world.boss.phase).toBe('intro');
    run(world, sys, BOSS_DEFS['profaner-knight'].introMs + 32);
    expect(world.boss.phase).toBe('p1');
    expect(world.boss.moveSpeed).toBe(BOSS_DEFS['profaner-knight'].phases[0].moveSpeed);
  });

  it('não move durante a intro; move em p1 na direção do jogador', () => {
    const world = createWorld(1);
    world.player.pos.x = 0;
    world.player.pos.y = 0;
    const sys = new BossSystem('profaner-knight', 0);
    run(world, sys, 16);
    world.boss.pos.x = 200;
    world.boss.pos.y = 0;
    const xIntro = world.boss.pos.x;
    run(world, sys, 200); // ainda intro
    expect(world.boss.pos.x).toBe(xIntro);
    run(world, sys, BOSS_DEFS['profaner-knight'].introMs + 1000); // p1, anda
    expect(world.boss.pos.x).toBeLessThan(xIntro);
  });

  it('transiciona de fase pelos limiares de hp e emite boss:phase', () => {
    const world = createWorld(1);
    const phaseEv = vi.fn();
    world.events.on('boss:phase', phaseEv);
    const sys = new BossSystem('profaner-knight', 0);
    run(world, sys, BOSS_DEFS['profaner-knight'].introMs + 64); // -> p1
    expect(world.boss.phase).toBe('p1');
    world.boss.hp = world.boss.maxHp * 0.5; // <= p2At 0.66
    run(world, sys, 32);
    expect(world.boss.phase).toBe('p2');
    world.boss.hp = world.boss.maxHp * 0.2; // <= enrageAt 0.33
    run(world, sys, 32);
    expect(world.boss.phase).toBe('enraged');
    expect(phaseEv).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/systems/bossSystem-state.test.ts`
Expected: FAIL — módulo não encontrado.

- [ ] **Step 3: Implementar `src/systems/BossSystem.ts`**

```ts
import type { System } from './System';
import type { World } from '../world/World';
import type { Boss, BossPhase } from '../entities/Boss';
import { BOSS_DEFS, type BossDef, type BossPhaseDef } from '../data/bosses';
import { SPAWN_RING_RADIUS } from '../config/gameConfig';

/** Faz o spawn do chefe no tempo certo e roda a máquina de estados dele. */
export class BossSystem implements System {
  private spawned = false;

  constructor(
    private readonly bossId: string,
    private readonly bossTimeSec: number,
  ) {}

  update(world: World, deltaMs: number): void {
    if (!this.spawned && world.time.elapsedMs >= this.bossTimeSec * 1000) {
      this.spawned = true;
      const def = BOSS_DEFS[this.bossId];
      const ang = world.rng.next() * Math.PI * 2;
      world.boss.spawn(
        def,
        world.player.pos.x + Math.cos(ang) * SPAWN_RING_RADIUS,
        world.player.pos.y + Math.sin(ang) * SPAWN_RING_RADIUS,
      );
      world.events.emit('boss:spawned', { defId: this.bossId });
    }

    const b = world.boss;
    if (!b.active) return;
    const def = BOSS_DEFS[b.defId];
    b.phaseTimeMs += deltaMs;

    if (b.phase === 'intro') {
      if (b.phaseTimeMs >= def.introMs) this.enterPhase(world, b, 'p1', def.phases[0]);
      return;
    }
    if (b.phase === 'dead') return;

    const frac = b.hp / b.maxHp;
    if (b.phase === 'p1' && frac <= def.p2At) {
      this.enterPhase(world, b, 'p2', def.phases[1]);
    } else if ((b.phase === 'p1' || b.phase === 'p2') && frac <= def.enrageAt) {
      this.enterPhase(world, b, 'enraged', def.phases[2]);
    }

    this.moveToward(b, world, deltaMs);

    b.attackCdMs -= deltaMs;
    // execução dos ataques entra na Task 5
  }

  private enterPhase(world: World, b: Boss, phase: BossPhase, pd: BossPhaseDef): void {
    b.phase = phase;
    b.phaseTimeMs = 0;
    b.attackIndex = 0;
    b.moveSpeed = pd.moveSpeed;
    b.attackCdMs = pd.attacks[0]?.cooldownMs ?? 2000;
    world.events.emit('boss:phase', { phase });
  }

  private moveToward(b: Boss, world: World, deltaMs: number): void {
    const dx = world.player.pos.x - b.pos.x;
    const dy = world.player.pos.y - b.pos.y;
    const d = Math.hypot(dx, dy) || 1;
    const stop = b.radius + world.player.radius + 4;
    if (d <= stop) return;
    const step = (b.moveSpeed * deltaMs) / 1000;
    b.pos.x += (dx / d) * step;
    b.pos.y += (dy / d) * step;
  }

  /** Fase-def da fase atual (para a Task 5). */
  protected phaseDef(b: Boss, def: BossDef): BossPhaseDef {
    if (b.phase === 'p2') return def.phases[1];
    if (b.phase === 'enraged') return def.phases[2];
    return def.phases[0];
  }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npx vitest run tests/systems/bossSystem-state.test.ts`
Expected: PASS — 4 testes verdes.

- [ ] **Step 5: Commit**

```bash
git add src/systems/BossSystem.ts tests/systems/bossSystem-state.test.ts
git commit -m "feat(boss): BossSystem spawn + phase machine + chase movement"
```

---

### Task 5: BossSystem — execução dos ataques

**Files:**
- Modify: `src/systems/BossSystem.ts`
- Test: `tests/systems/bossSystem-attacks.test.ts`

**Interfaces:**
- Consumes: `world.attacks`, `world.enemies`, `ENEMY_DEFS` (`data/enemies`), `BossAttack` (`data/bosses`).
- Produces:
  - No `update`, quando `b.attackCdMs <= 0` (e fora de `intro`/`dead`): pega `atk = phaseDef.attacks[b.attackIndex % phaseDef.attacks.length]`, executa conforme `atk.kind`, `b.attackIndex++`, `b.attackCdMs = atk.cooldownMs`.
  - `ring`: `atk.count` `Attack`s do pool, motion `'linear'`, saindo de `boss.pos` em ângulos uniformes (`i/count * 2π`), `vel = dir * atk.speed`, `damage = atk.damage`, `radius = atk.radius ?? 5`, `pierceLeft = 0`, `hitCooldownMs = 0`, `lifespanMs = 4000`, `ownerPowerId = 'boss'`, `spriteKey = 'dev-spear'`. Para se `acquire()` devolver `null`.
  - `charge`: 1 `Attack` linear de `boss.pos` em direção ao jogador, `vel = dir * atk.speed`, `damage = atk.damage`, `radius = atk.radius ?? 8`, `pierceLeft = 3`, `hitCooldownMs = 0`, `lifespanMs = 1800`, `ownerPowerId = 'boss'`, `spriteKey = 'dev-spear'`.
  - `nova`: 1 `Attack` motion `'static'` "colado" no chefe? Não — `nova` explode onde o chefe está: `Attack` estático em `boss.pos` (mas `motion 'static'` gruda no jogador). Use `motion 'linear'` com `vel` zero, `pos = boss.pos`, `radius = atk.radius ?? 46`, `damage = atk.damage`, `hitCooldownMs = 400`, `lifespanMs = 600`, `ownerPowerId = 'boss'`, `spriteKey = 'dev-aura'`.
  - `summon`: `atk.count` inimigos de `world.enemies`, `spawn(ENEMY_DEFS[atk.archetype ?? 'runner'], boss.pos.x + offset, boss.pos.y + offset)` com pequeno espalhamento por `world.rng`. Para se `acquire()` devolver `null`.

- [ ] **Step 1: Escrever `tests/systems/bossSystem-attacks.test.ts` (falha)**

```ts
import { describe, it, expect } from 'vitest';
import { createWorld, advanceTime } from '../../src/world/World';
import { BossSystem } from '../../src/systems/BossSystem';
import { BOSS_DEFS } from '../../src/data/bosses';

function toPhase1(world: ReturnType<typeof createWorld>, sys: BossSystem, id: string): void {
  for (let t = 0; t < BOSS_DEFS[id].introMs + 64; t += 16) {
    advanceTime(world, 16);
    sys.update(world, 16);
  }
}

describe('BossSystem — ataques', () => {
  it('ring: gera atk.count projéteis lineares com ownerPowerId "boss"', () => {
    const world = createWorld(1);
    const sys = new BossSystem('the-first-betrayed', 0); // fase 1 = [ring()]
    toPhase1(world, sys, 'the-first-betrayed');
    world.boss.attackCdMs = 0;
    sys.update(world, 16);
    let bolts = 0;
    world.attacks.forEachActive((a) => {
      if (a.ownerPowerId === 'boss' && a.motion === 'linear') bolts++;
    });
    expect(bolts).toBe(BOSS_DEFS['the-first-betrayed'].phases[0].attacks[0].count);
  });

  it('charge: gera 1 projétil rápido em direção ao jogador', () => {
    const world = createWorld(1);
    world.player.pos.x = 300;
    const sys = new BossSystem('profaner-knight', 0); // fase 1 = [charge()]
    toPhase1(world, sys, 'profaner-knight');
    world.boss.pos.x = 0;
    world.boss.pos.y = 0;
    world.boss.attackCdMs = 0;
    sys.update(world, 16);
    let found = false;
    world.attacks.forEachActive((a) => {
      if (a.ownerPowerId === 'boss') {
        found = true;
        expect(a.vel.x).toBeGreaterThan(0); // vai para +x (jogador)
      }
    });
    expect(found).toBe(true);
  });

  it('summon: invoca atk.count inimigos', () => {
    const world = createWorld(1);
    const sys = new BossSystem('grand-inquisitor', 0);
    toPhase1(world, sys, 'grand-inquisitor'); // fase 1 = [nova()]
    // força a fase 2 (tem summon)
    world.boss.hp = world.boss.maxHp * 0.5;
    advanceTime(world, 16);
    sys.update(world, 16); // entra em p2
    // roda até um summon sair
    let summoned = 0;
    for (let i = 0; i < 800; i++) {
      advanceTime(world, 16);
      sys.update(world, 16);
      summoned = world.enemies.activeCount;
      if (summoned > 0) break;
    }
    expect(summoned).toBeGreaterThan(0);
  });

  it('respeita o cooldown entre ataques', () => {
    const world = createWorld(1);
    const sys = new BossSystem('the-first-betrayed', 0);
    toPhase1(world, sys, 'the-first-betrayed');
    world.boss.attackCdMs = 0;
    sys.update(world, 16);
    const after1 = world.attacks.activeCount;
    advanceTime(world, 100);
    sys.update(world, 100);
    expect(world.attacks.activeCount).toBe(after1); // ainda em cooldown
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/systems/bossSystem-attacks.test.ts`
Expected: FAIL — nenhum ataque é executado ainda.

- [ ] **Step 3: Editar `src/systems/BossSystem.ts`**

Adicionar os imports:

```ts
import { ENEMY_DEFS } from '../data/enemies';
import type { BossAttack } from '../data/bosses';
```

No `update`, substituir a linha `b.attackCdMs -= deltaMs;` e o comentário seguinte por:

```ts
    b.attackCdMs -= deltaMs;
    if (b.attackCdMs <= 0) {
      const pd = this.phaseDef(b, def);
      const atk = pd.attacks[b.attackIndex % pd.attacks.length];
      this.execute(world, atk);
      b.attackIndex++;
      b.attackCdMs = atk.cooldownMs;
    }
```

Adicionar o método `execute` à classe:

```ts
  private execute(world: World, atk: BossAttack): void {
    const b = world.boss;
    if (atk.kind === 'ring') {
      const n = atk.count ?? 12;
      for (let i = 0; i < n; i++) {
        const a = world.attacks.acquire();
        if (!a) break;
        const ang = (i / n) * Math.PI * 2;
        a.motion = 'linear';
        a.pos.x = b.pos.x;
        a.pos.y = b.pos.y;
        a.vel.x = Math.cos(ang) * (atk.speed ?? 90);
        a.vel.y = Math.sin(ang) * (atk.speed ?? 90);
        a.damage = atk.damage;
        a.radius = atk.radius ?? 5;
        a.pierceLeft = 0;
        a.hitCooldownMs = 0;
        a.lifespanMs = 4000;
        a.ownerPowerId = 'boss';
        a.spriteKey = 'dev-spear';
      }
    } else if (atk.kind === 'charge') {
      const a = world.attacks.acquire();
      if (!a) return;
      const dx = world.player.pos.x - b.pos.x;
      const dy = world.player.pos.y - b.pos.y;
      const d = Math.hypot(dx, dy) || 1;
      a.motion = 'linear';
      a.pos.x = b.pos.x;
      a.pos.y = b.pos.y;
      a.vel.x = (dx / d) * (atk.speed ?? 260);
      a.vel.y = (dy / d) * (atk.speed ?? 260);
      a.damage = atk.damage;
      a.radius = atk.radius ?? 8;
      a.pierceLeft = 3;
      a.hitCooldownMs = 0;
      a.lifespanMs = 1800;
      a.ownerPowerId = 'boss';
      a.spriteKey = 'dev-spear';
    } else if (atk.kind === 'nova') {
      const a = world.attacks.acquire();
      if (!a) return;
      a.motion = 'linear';
      a.pos.x = b.pos.x;
      a.pos.y = b.pos.y;
      a.vel.x = 0;
      a.vel.y = 0;
      a.damage = atk.damage;
      a.radius = atk.radius ?? 46;
      a.hitCooldownMs = 400;
      a.lifespanMs = 600;
      a.ownerPowerId = 'boss';
      a.spriteKey = 'dev-aura';
    } else if (atk.kind === 'summon') {
      const n = atk.count ?? 4;
      const def = ENEMY_DEFS[atk.archetype ?? 'runner'];
      for (let i = 0; i < n; i++) {
        const e = world.enemies.acquire();
        if (!e) break;
        const ox = (world.rng.next() - 0.5) * 40;
        const oy = (world.rng.next() - 0.5) * 40;
        e.spawn(def, b.pos.x + ox, b.pos.y + oy);
      }
    }
  }
```

- [ ] **Step 4: Rodar e confirmar que passa + typecheck**

Run: `npx vitest run tests/systems/bossSystem-attacks.test.ts`
Expected: PASS — 4 testes verdes.

Run: `npm run typecheck`
Expected: sem erros.

- [ ] **Step 5: Commit**

```bash
git add src/systems/BossSystem.ts tests/systems/bossSystem-attacks.test.ts
git commit -m "feat(boss): boss attack patterns (ring/charge/nova/summon)"
```

---

### Task 6: Dano nos dois sentidos (jogador ↔ chefe)

**Files:**
- Modify: `src/systems/AttackCollisionSystem.ts`
- Modify: `src/systems/PlayerAttackSystem.ts`
- Modify: `src/systems/ContactDamageSystem.ts`
- Test: `tests/systems/bossDamage.test.ts`

**Interfaces:**
- Consumes: `damageBoss` (`combat/boss`), `IFRAME_MS`/`CLAW_RANGE`/`CLAW_DAMAGE` (`gameConfig`).
- Produces:
  - `AttackCollisionSystem`: para cada `Attack` ativo —
    - se `a.ownerPowerId === 'boss'`: testa overlap com o **jogador**; se colidir e `now >= player.invulnUntilMs`, `dmg = max(1, a.damage - armor)`, `player.hp -= dmg`, `player.invulnUntilMs = now + IFRAME_MS`, emite `player:damaged` (e `player:died` se `hp <= 0`, com `hp` clampado em 0); se `a.hitCooldownMs === 0` decrementa `pierceLeft` e libera quando `< 0`. **Não** testa contra inimigos.
    - senão (ataque do jogador): mantém o loop contra `world.enemies` **e** testa contra `world.boss` se `active` — respeitando `hitCooldownMs` via `a.bossHitAtMs` (para hitbox persistente) ou `pierceLeft` (para projétil); no acerto chama `damageBoss(world, a.damage)`.
  - `PlayerAttackSystem`: depois do loop de inimigos, se `world.boss.active` e a distância chefe↔jogador `<= CLAW_RANGE`, `damageBoss(world, CLAW_DAMAGE)`.
  - `ContactDamageSystem`: depois do loop de inimigos, se `world.boss.active`, `boss.phase !== 'intro'`, `now >= player.invulnUntilMs` e overlap jogador↔chefe: `dmg = max(1, boss.contactDamage - armor)`, aplica igual ao dano de inimigo (i-frames, eventos).

- [ ] **Step 1: Escrever `tests/systems/bossDamage.test.ts` (falha)**

```ts
import { describe, it, expect, vi } from 'vitest';
import { createWorld } from '../../src/world/World';
import { AttackCollisionSystem } from '../../src/systems/AttackCollisionSystem';
import { PlayerAttackSystem } from '../../src/systems/PlayerAttackSystem';
import { ContactDamageSystem } from '../../src/systems/ContactDamageSystem';
import { BOSS_DEFS } from '../../src/data/bosses';
import { CLAW_DAMAGE } from '../../src/config/gameConfig';

function bossAt(world: ReturnType<typeof createWorld>, x: number, y: number) {
  world.boss.spawn(BOSS_DEFS['profaner-knight'], x, y);
  world.boss.phase = 'p1'; // fora da intro
}

describe('dano jogador -> chefe', () => {
  it('projétil do jogador fere o chefe', () => {
    const world = createWorld(1);
    bossAt(world, 0, 0);
    const a = world.attacks.acquire()!;
    a.ownerPowerId = 'blood-spear';
    a.motion = 'linear';
    a.radius = 6;
    a.damage = 30;
    a.hitCooldownMs = 0;
    a.pierceLeft = 0;
    new AttackCollisionSystem().update(world);
    expect(world.boss.hp).toBe(BOSS_DEFS['profaner-knight'].hp - 30);
    expect(world.attacks.activeCount).toBe(0); // projétil consumido
  });

  it('a garra automática fere o chefe no alcance', () => {
    const world = createWorld(1);
    bossAt(world, 5, 0);
    new PlayerAttackSystem().update(world);
    expect(world.boss.hp).toBe(BOSS_DEFS['profaner-knight'].hp - CLAW_DAMAGE);
  });
});

describe('dano chefe -> jogador', () => {
  it('ataque com ownerPowerId "boss" fere o jogador e não os inimigos', () => {
    const world = createWorld(1);
    const damaged = vi.fn();
    world.events.on('player:damaged', damaged);
    const a = world.attacks.acquire()!;
    a.ownerPowerId = 'boss';
    a.motion = 'linear';
    a.radius = 8;
    a.damage = 15;
    a.hitCooldownMs = 0;
    a.pierceLeft = 0;
    new AttackCollisionSystem().update(world); // ataque em (0,0), jogador em (0,0)
    expect(world.player.hp).toBe(85);
    expect(damaged).toHaveBeenCalled();
  });

  it('encostar no chefe ativo causa dano de contato', () => {
    const world = createWorld(1);
    bossAt(world, world.player.pos.x, world.player.pos.y);
    new ContactDamageSystem().update(world);
    expect(world.player.hp).toBe(100 - BOSS_DEFS['profaner-knight'].contactDamage);
  });

  it('não toma dano de contato do chefe na intro', () => {
    const world = createWorld(1);
    world.boss.spawn(BOSS_DEFS['profaner-knight'], world.player.pos.x, world.player.pos.y); // intro
    new ContactDamageSystem().update(world);
    expect(world.player.hp).toBe(100);
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/systems/bossDamage.test.ts`
Expected: FAIL — nenhum caminho de dano jogador↔chefe existe.

- [ ] **Step 3: Substituir `src/systems/AttackCollisionSystem.ts` inteiro**

```ts
import type { System } from './System';
import type { World } from '../world/World';
import { killEnemy } from '../combat/kill';
import { damageBoss } from '../combat/boss';
import { IFRAME_MS } from '../config/gameConfig';

/** Resolve overlaps de Attack: ataques do jogador ferem inimigos e o chefe; ataques 'boss' ferem o jogador. */
export class AttackCollisionSystem implements System {
  update(world: World): void {
    const now = world.time.elapsedMs;

    world.attacks.forEachActive((a) => {
      if (a.ownerPowerId === 'boss') {
        this.hitPlayer(world, a, now);
        return;
      }

      world.enemies.forEachActive((e) => {
        if (!a.active) return;
        const rr = a.radius + e.radius;
        const dx = e.pos.x - a.pos.x;
        const dy = e.pos.y - a.pos.y;
        if (dx * dx + dy * dy > rr * rr) return;

        if (a.hitCooldownMs > 0) {
          const last = a.hits.get(e) ?? -Infinity;
          if (now - last < a.hitCooldownMs) return;
          a.hits.set(e, now);
        }

        e.hp -= a.damage;
        if (e.hp <= 0) killEnemy(world, e);

        if (a.hitCooldownMs === 0) {
          a.pierceLeft -= 1;
          if (a.pierceLeft < 0) world.attacks.release(a);
        }
      });

      if (a.active && world.boss.active) this.hitBoss(world, a, now);
    });
  }

  private hitPlayer(world: World, a: import('../entities/Attack').Attack, now: number): void {
    const p = world.player;
    if (now < p.invulnUntilMs) return;
    const rr = a.radius + p.radius;
    const dx = p.pos.x - a.pos.x;
    const dy = p.pos.y - a.pos.y;
    if (dx * dx + dy * dy > rr * rr) return;

    const dmg = Math.max(1, a.damage - p.stats.get('armor'));
    p.hp -= dmg;
    p.invulnUntilMs = now + IFRAME_MS;
    world.events.emit('player:damaged', { amount: dmg, hpRemaining: p.hp });
    if (p.hp <= 0) {
      p.hp = 0;
      world.events.emit('player:died', {});
    }
    if (a.hitCooldownMs === 0) {
      a.pierceLeft -= 1;
      if (a.pierceLeft < 0) world.attacks.release(a);
    }
  }

  private hitBoss(world: World, a: import('../entities/Attack').Attack, now: number): void {
    const b = world.boss;
    const rr = a.radius + b.radius;
    const dx = b.pos.x - a.pos.x;
    const dy = b.pos.y - a.pos.y;
    if (dx * dx + dy * dy > rr * rr) return;

    if (a.hitCooldownMs > 0) {
      if (now - a.bossHitAtMs < a.hitCooldownMs) return;
      a.bossHitAtMs = now;
    }
    damageBoss(world, a.damage);
    if (a.hitCooldownMs === 0) {
      a.pierceLeft -= 1;
      if (a.pierceLeft < 0) world.attacks.release(a);
    }
  }
}
```

- [ ] **Step 4: Editar `src/systems/PlayerAttackSystem.ts`**

Adicionar o import:

```ts
import { damageBoss } from '../combat/boss';
```

Dentro de `update`, logo após o `world.enemies.forEachActive(...)` que aplica a garra, adicionar:

```ts
    if (world.boss.active) {
      const bdx = world.boss.pos.x - px;
      const bdy = world.boss.pos.y - py;
      if (bdx * bdx + bdy * bdy <= range2) damageBoss(world, CLAW_DAMAGE);
    }
```

(`px`, `py`, `range2` já existem no método; `CLAW_DAMAGE` já está importado.)

- [ ] **Step 5: Editar `src/systems/ContactDamageSystem.ts`**

Logo após o `world.enemies.forEachActive(...)`, ainda dentro de `update`, adicionar:

```ts
    const b = world.boss;
    if (b.active && b.phase !== 'intro' && world.time.elapsedMs >= p.invulnUntilMs) {
      const dx = b.pos.x - p.pos.x;
      const dy = b.pos.y - p.pos.y;
      const rr = p.radius + b.radius;
      if (dx * dx + dy * dy <= rr * rr) {
        const dmg = Math.max(1, b.contactDamage - p.stats.get('armor'));
        p.hp -= dmg;
        p.invulnUntilMs = world.time.elapsedMs + IFRAME_MS;
        world.events.emit('player:damaged', { amount: dmg, hpRemaining: p.hp });
        if (p.hp <= 0) {
          p.hp = 0;
          world.events.emit('player:died', {});
        }
      }
    }
```

(`p`, `IFRAME_MS` já disponíveis no método.)

- [ ] **Step 6: Rodar e confirmar que passa + suíte + typecheck**

Run: `npx vitest run tests/systems/bossDamage.test.ts`
Expected: PASS — 6 testes verdes.

Run: `npm test && npm run typecheck`
Expected: tudo verde (os testes herdados de `attackCollision`/`contactDamage`/`playerAttack` continuam válidos: sem chefe ativo, os novos ramos não rodam).

- [ ] **Step 7: Commit**

```bash
git add src/systems/AttackCollisionSystem.ts src/systems/PlayerAttackSystem.ts src/systems/ContactDamageSystem.ts tests/systems/bossDamage.test.ts
git commit -m "feat(boss): two-way damage — player attacks hit the boss, boss attacks hit the player"
```

---

### Task 7: Memórias com chefe + vitória por derrotá-lo + SpawnDirector

**Files:**
- Modify: `src/data/memories.ts` (`bossId`/`bossTimeSec`)
- Modify: `src/run/runEnd.ts` (vitória por `bossDefeated`)
- Modify: `src/systems/SpawnDirector.ts` (pausa com o chefe)
- Modify: `tests/data/memories.test.ts` (checar `bossId`)
- Test: `tests/run/runEnd-boss.test.ts`
- Test: `tests/systems/spawnDirector-boss.test.ts`

**Interfaces:**
- Consumes: `World`, `Boss`.
- Produces:
  - `MemoryDef` ganha `bossId: string` e `bossTimeSec: number`. As 3 memórias: `m1` → `'profaner-knight'` / `60`; `m2` → `'grand-inquisitor'` / `70`; `m3` → `'janissary-commander'` / `80`.
  - `runOutcome(world, memoryDurationSec)`: `player.hp <= 0` → `'defeat'`; `world.bossDefeated` → `'victory'`; `time.elapsedMs >= durationSec*1000` → `'victory'`; senão `'running'`.
  - `SpawnDirector.update`: se `world.boss.active`, retorna no início (não surge inimigos comuns; só o `BossSystem` invoca durante o confronto).

- [ ] **Step 1: Escrever/editar os testes**

Adicionar ao `describe('MEMORIES', ...)` em `tests/data/memories.test.ts`:

```ts
  it('toda memória tem um chefe e um tempo de chefe', () => {
    for (const m of MEMORIES) {
      expect(m.bossId).toBeTruthy();
      expect(m.bossTimeSec).toBeGreaterThan(0);
    }
  });
```

```ts
// tests/run/runEnd-boss.test.ts
import { describe, it, expect } from 'vitest';
import { createWorld, advanceTime } from '../../src/world/World';
import { runOutcome } from '../../src/run/runEnd';

describe('runOutcome com chefe', () => {
  it('victory quando bossDefeated, mesmo antes do tempo', () => {
    const world = createWorld(1);
    advanceTime(world, 5000);
    world.bossDefeated = true;
    expect(runOutcome(world, 300)).toBe('victory');
  });

  it('defeat continua tendo prioridade', () => {
    const world = createWorld(1);
    world.bossDefeated = true;
    world.player.hp = 0;
    expect(runOutcome(world, 300)).toBe('defeat');
  });

  it('running quando o chefe ainda não caiu e o tempo não acabou', () => {
    const world = createWorld(1);
    advanceTime(world, 1000);
    expect(runOutcome(world, 300)).toBe('running');
  });
});
```

```ts
// tests/systems/spawnDirector-boss.test.ts
import { describe, it, expect } from 'vitest';
import { createWorld, advanceTime } from '../../src/world/World';
import { SpawnDirector } from '../../src/systems/SpawnDirector';
import { BOSS_DEFS } from '../../src/data/bosses';

describe('SpawnDirector pausa durante o chefe', () => {
  it('não surge inimigos comuns enquanto world.boss.active', () => {
    const world = createWorld(1);
    const dir = new SpawnDirector([{ tSec: 0, budget: 20, pool: ['crawler'] }]);
    world.boss.spawn(BOSS_DEFS['profaner-knight'], 500, 0);
    for (let t = 0; t < 5000; t += 16) {
      advanceTime(world, 16);
      dir.update(world);
    }
    expect(world.enemies.activeCount).toBe(0);
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falham**

Run: `npx vitest run tests/data/memories.test.ts tests/run/runEnd-boss.test.ts tests/systems/spawnDirector-boss.test.ts`
Expected: FAIL — `bossId` ausente / `runOutcome` não vê `bossDefeated` / SpawnDirector não pausa.

- [ ] **Step 3: Editar `src/data/memories.ts`**

Na `interface MemoryDef`, após `durationSec: number;`:
```ts
  bossId: string;
  bossTimeSec: number;
```
Em cada entrada de `MEMORIES`, acrescentar os campos: `m1` `bossId: 'profaner-knight', bossTimeSec: 60`; `m2` `bossId: 'grand-inquisitor', bossTimeSec: 70`; `m3` `bossId: 'janissary-commander', bossTimeSec: 80`.

- [ ] **Step 4: Editar `src/run/runEnd.ts`**

```ts
export function runOutcome(world: World, memoryDurationSec: number): RunOutcome {
  if (world.player.hp <= 0) return 'defeat';
  if (world.bossDefeated) return 'victory';
  if (world.time.elapsedMs >= memoryDurationSec * 1000) return 'victory';
  return 'running';
}
```

- [ ] **Step 5: Editar `src/systems/SpawnDirector.ts`**

No início de `update(world: World)`, antes de qualquer outra coisa:

```ts
    if (world.boss.active) return;
```

- [ ] **Step 6: Rodar e confirmar que passam + suíte + typecheck**

Run: `npx vitest run tests/data/memories.test.ts tests/run/runEnd-boss.test.ts tests/systems/spawnDirector-boss.test.ts`
Expected: PASS.

Run: `npm test && npm run typecheck`
Expected: tudo verde.

- [ ] **Step 7: Commit**

```bash
git add src/data/memories.ts src/run/runEnd.ts src/systems/SpawnDirector.ts tests/data/memories.test.ts tests/run/runEnd-boss.test.ts tests/systems/spawnDirector-boss.test.ts
git commit -m "feat(boss): memories carry a boss; killing it wins; spawns pause during the fight"
```

---

### Task 8: Arte do chefe M1 + fiação na RunScene + integração

**Files:**
- Modify: `tools/process-sprites.mjs` (job `boss-m1`)
- Modify: `src/scenes/PreloadScene.ts` (`dev-boss`)
- Modify: `src/scenes/RunScene.ts` (`BossSystem` + sprite do chefe + texto de depuração)
- Test: `tests/integration/bossFight.test.ts`

**Interfaces:**
- Consumes: `BossSystem` (Tasks 4–5), `BOSS_DEFS` (Task 1), `runOutcome` (Task 7), `MEMORIES` (Task 7), `makeWorld`/`tick` (helpers), sistemas dos Planos 2–4.
- Produces: nada consumido depois (ponta visível + rede de integração).

- [ ] **Step 1: Escrever `tests/integration/bossFight.test.ts` (falha)**

```ts
import { describe, it, expect } from 'vitest';
import { makeWorld, tick } from '../helpers/headlessWorld';
import { MEMORIES } from '../../src/data/memories';
import { BossSystem } from '../../src/systems/BossSystem';
import { PowerSystem } from '../../src/systems/PowerSystem';
import { AttackMotionSystem } from '../../src/systems/AttackMotionSystem';
import { AttackCollisionSystem } from '../../src/systems/AttackCollisionSystem';
import { EnemyMovementSystem } from '../../src/systems/EnemyMovementSystem';
import { SpawnDirector } from '../../src/systems/SpawnDirector';
import { ContactDamageSystem } from '../../src/systems/ContactDamageSystem';
import { runOutcome } from '../../src/run/runEnd';

describe('confronto de chefe (integração headless)', () => {
  it('M1: chefe surge, toma dano dos poderes e cai -> victory', () => {
    const m = MEMORIES[0];
    const world = makeWorld(2024);
    world.powers.equip('bat-swarm');
    for (let i = 0; i < 7; i++) world.powers.levelUp('bat-swarm'); // L8
    world.player.stats.setBase('might', 400); // acelera o teste

    const boss = new BossSystem(m.bossId, m.bossTimeSec);
    const sys = [
      new SpawnDirector(m.timeline),
      boss,
      new PowerSystem(),
      new AttackMotionSystem(),
      new AttackCollisionSystem(),
      new EnemyMovementSystem(),
      new ContactDamageSystem(),
    ];

    // antes do bossTimeSec: sem chefe
    tick(world, sys, Math.floor((m.bossTimeSec * 1000) / 16) - 5, 16);
    expect(world.boss.active).toBe(false);

    // depois: chefe surge e é farmado pelos orbes (com might altíssimo)
    tick(world, sys, 60 * 60, 16); // até 60s de luta
    expect(world.bossDefeated).toBe(true);
    expect(runOutcome(world, m.durationSec)).toBe('victory');
  });

  it('nenhum inimigo comum surge durante o confronto', () => {
    const m = MEMORIES[0];
    const world = makeWorld(7);
    const boss = new BossSystem(m.bossId, 0); // surge já
    const sys = [new SpawnDirector(m.timeline), boss, new EnemyMovementSystem()];
    tick(world, sys, 300, 16); // intro + um pouco
    // após a intro o chefe pode invocar, mas o SpawnDirector não
    const beforeSummons = world.enemies.activeCount;
    expect(beforeSummons).toBeLessThan(5); // sem enxame do director
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/integration/bossFight.test.ts`
Expected: FAIL enquanto algum import estiver quebrado; se as Tasks 1–7 já estiverem prontas, pode passar — siga ao Step 3 (a parte visual falta).

- [ ] **Step 3: Inspecionar e processar `boss_m1_profaner_knight.jpg`**

Abrir a imagem (`sprites/boss_m1_profaner_knight.jpg`, 2816×1536) e decidir o layout de células. Acrescentar a `JOBS` em `tools/process-sprites.mjs` uma entrada:

```js
  { src: 'boss_m1_profaner_knight.jpg', name: 'boss-m1', cols: <C>, rows: <R>, take: <N>, targetH: 96 },
```

- Se for uma grade legível de poses, use `cols`/`rows` reais e `take` o nº de frames aproveitáveis.
- Se não der para ler a grade, use `{ cols: 1, rows: 1, targetH: 96 }` (um único frame representativo).

Rodar `npm run sprites` e conferir `public/sprites/boss-m1.png` + a entrada em `manifest.json`.

- [ ] **Step 4: Editar `src/scenes/PreloadScene.ts`**

No fim de `makeDevTextures()`, adicionar:

```ts
    const boss = this.add.graphics();
    boss.fillStyle(0x2a0f14, 1);
    boss.fillRect(0, 0, 40, 52);
    boss.fillStyle(0xb31217, 1);
    boss.fillRect(6, 10, 28, 10);
    boss.lineStyle(2, 0xf05030, 1);
    boss.strokeRect(1, 1, 38, 50);
    boss.generateTexture('dev-boss', 40, 52);
    boss.destroy();
```

- [ ] **Step 5: Editar `src/scenes/RunScene.ts`**

Imports:
```ts
import { BossSystem } from '../systems/BossSystem';
import { BOSS_DEFS } from '../data/bosses';
```

Campo novo na classe:
```ts
  private bossSprite?: Phaser.GameObjects.Sprite;
```

Em `create()`, na lista `this.systems`, inserir o `BossSystem` logo depois de `new DashSystem(input),`:
```ts
      new BossSystem(this.memory.bossId, this.memory.bossTimeSec),
```

Ainda em `create()`, registrar a animação do chefe M1 (junto das outras `registerAnim`), se existir:
```ts
    this.registerAnim('boss-m1', 6, -1);
```

Em `update()`, depois de `this.syncAttacks();`, adicionar `this.syncBoss();`. E acrescentar o método:

```ts
  private syncBoss(): void {
    const b = this.world.boss;
    if (!b.active) {
      this.bossSprite?.setVisible(false);
      return;
    }
    const def = BOSS_DEFS[b.defId];
    const key = this.textures.exists(def.spriteKey) ? def.spriteKey : 'dev-boss';
    if (!this.bossSprite) {
      this.bossSprite = this.add.sprite(0, 0, key).setDepth(6);
    }
    if (this.bossSprite.texture.key !== key) {
      this.bossSprite.setTexture(key);
      if (this.anims.exists(key)) this.bossSprite.play(key, true);
    }
    this.bossSprite.setVisible(true).setPosition(b.pos.x, b.pos.y);
    this.bossSprite.setFlipX(this.world.player.pos.x < b.pos.x);
  }
```

Atualizar o `debugText.setText(...)` para incluir o estado do chefe:

```ts
    const bossInfo = this.world.boss.active
      ? `  CHEFE ${this.world.boss.phase} ${Math.ceil(this.world.boss.hp)}/${this.world.boss.maxHp}`
      : this.world.bossDefeated
        ? '  CHEFE DERROTADO'
        : '';
    this.debugText.setText(
      `HP ${Math.ceil(p.hp)}  Lv ${this.world.progression.level}  ` +
        `abates ${this.kills}  ${remaining}s  poderes ${this.world.powers.count()}${bossInfo}`,
    );
```

- [ ] **Step 6: Typecheck + build + suíte**

Run: `npm run typecheck`
Expected: sem erros.

Run: `npm run build`
Expected: `dist/` gerado.

Run: `npm test`
Expected: PASS — incluindo `tests/integration/bossFight.test.ts`.

- [ ] **Step 7: Verificação no navegador (frames bombeados)**

Run: `npm run dev`, abrir `http://localhost:5173`, **frontar a aba**.

No console:

```js
const g = window.__GAME__;
let t = performance.now();
for (let i = 0; i < 800; i++) { t += 16; g.step(t, 16); } // -> Hub
const hub = g.scene.getScene('Hub');
hub.registry.set('save', { ...hub.registry.get('save'), essence: 0 });
const memRow = hub.rows.find(o => o.type === 'Text' && o.input && /▶/.test(o.text));
memRow.emit('pointerup');
for (let i = 0; i < 60; i++) { t += 16; g.step(t, 16); }
const run = g.scene.getScene('Run');
run.world.player.stats.setBase('might', 800); // acelera
run.world.powers.equip('bat-swarm');
for (let k = 0; k < 7; k++) run.world.powers.levelUp('bat-swarm');
// pula para logo antes do chefe
run.world.time.elapsedMs = run.memory.bossTimeSec * 1000 - 500;
for (let i = 0; i < 600; i++) { t += 16; g.step(t, 16); } // chefe surge + intro
const s1 = { bossActive: run.world.boss.active, phase: run.world.boss.phase, hp: Math.ceil(run.world.boss.hp) };
for (let i = 0; i < 3000; i++) { t += 16; g.step(t, 16); } // luta
const s2 = {
  bossDefeated: run.world.bossDefeated,
  activeScenes: g.scene.scenes.filter(x => x.scene.isActive()).map(x => x.scene.key),
  saveMemories: g.registry.get('save').memoriesCleared,
};
JSON.stringify({ s1, s2 }, null, 2);
```

Expected: `s1.bossActive` = `true` com `phase` saindo de `'intro'`; a vida do chefe cai; `s2.bossDefeated` = `true`, cenas ativas passam a `['RunEnd']`, e `saveMemories` contém `'m1'`. Tirar um screenshot mostrando o chefe (sprite `boss-m1` ou o `dev-boss`) e o texto de depuração com `CHEFE ...`. Encerrar o dev server.

- [ ] **Step 8: Commit**

```bash
git add tools/process-sprites.mjs public/sprites src/scenes/PreloadScene.ts src/scenes/RunScene.ts tests/integration/bossFight.test.ts
git commit -m "feat(game): wire BossSystem into RunScene + M1 boss art; killing the boss wins the memory"
```

---

## Self-Review (executado sobre este plano)

**1. Cobertura do escopo do Plano 5a** (roadmap do Plano 1 + design §5.6):
- Classe `Boss` com máquina de estados (`intro → p1 → p2 → enraged → dead`) → Tasks 1, 4. ✅
- 5 chefes + Satã em 3 fases como dados → Task 1. ✅ (As "3 fases" do Satã são as 3 entradas de `phases`; a narrativa das 3 formas é visual, do Plano 5b.)
- Chefe = condição de vitória → Task 7. ✅
- Chefe dá e recebe dano → Task 6. ✅
- Padrões de ataque (anel, investida, invocação, nova) → Task 5. ✅ (Telegrafo/windup dos ataques adiado — anotado; hoje o ataque sai direto.)
- Arte real só do chefe de M1 → Task 8. ✅
- Fora do Plano 5a → Plano 5b: HUD real (barra de vida do chefe), reskins de inimigo por memória, fundos parallax, i18n, telas de título/abertura, integração dos demais atlas.

**2. Varredura de placeholders:** sem "TBD/TODO". `dev-boss` é placeholder de produto para os 4 chefes sem arte; a Task 8 Step 3 tem um caminho explícito (`cols:1,rows:1`) para quando a grade da imagem não for legível.

**3. Consistência de tipos:**
- `Boss` campos (`active/pos/defId/hp/maxHp/radius/contactDamage/moveSpeed/phase/phaseTimeMs/attackCdMs/attackIndex`) — Task 1; usados por nome em `BossSystem` (4, 5), `damageBoss` (3), os 3 sistemas de dano (6), `RunScene` (8).
- `BossDef`/`BossPhaseDef`/`BossAttack`/`BossAttackKind` — Task 1; consumidos em 4, 5, 8.
- `BossPhase = 'intro'|'p1'|'p2'|'enraged'|'dead'` — Task 1; comparado literalmente em 3, 4, 5, 6, 8.
- `world.boss: Boss` e `world.bossDefeated: boolean` — Task 2; lidos por nome exato depois.
- Eventos `boss:spawned`/`boss:phase`/`boss:died` no `EventMap` (Task 2) — emitidos em 3 (`boss:died`), 4 (`boss:spawned`, `boss:phase`).
- `Attack.bossHitAtMs` — Task 3; lido só por `AttackCollisionSystem.hitBoss` (Task 6).
- `damageBoss(world, amount): void` — Task 3; chamado por `AttackCollisionSystem` e `PlayerAttackSystem` (6).
- `ownerPowerId === 'boss'` é o discriminador único entre ataque do chefe e do jogador — setado em `BossSystem.execute` (5), checado em `PowerSystem.resync` (3) e `AttackCollisionSystem` (6).
- `MemoryDef.bossId`/`bossTimeSec` — Task 7; `RunScene` passa ambos ao `new BossSystem(...)` (8); a integração lê de `MEMORIES[0]`.
- `runOutcome(world, durationSec)` — assinatura inalterada; passa a ler `world.bossDefeated` (7).

**4. Ordem dos sistemas na `RunScene`** (Task 8): SpawnDirector → Input → Movement → Dash → **BossSystem** → Power → AttackMotion → AttackCollision → EnemyMovement → PlayerAttack → ContactDamage → Regen → Pickup → Camera. BossSystem antes de Power para que os ataques do chefe e os do jogador sejam criados antes de AttackMotion/AttackCollision no mesmo frame.

---

## Depois do Plano 5a

**Plano 5b — HUD + i18n + arte das memórias:** `HUDScene` real (barra de vida do jogador e do chefe, XP, timer, ícones de poder), `src/i18n/pt.ts` com `t()`, `ParallaxBackground` com os layers de M1, reskins de inimigo por memória (processar `m1_risen_servant`→runner, `m1_crypt_skeleton`→brute), `TitleScene` com a arte-título, tela de abertura de memória com a ilustração, expansão do `tools/process-sprites.mjs`.
